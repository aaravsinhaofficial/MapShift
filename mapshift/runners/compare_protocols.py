"""Protocol comparison scaffold."""

from __future__ import annotations

from typing import Any, Dict


def compare_protocol_outputs(outputs: Dict[str, Any]) -> Dict[str, Any]:
    """Return the raw outputs keyed by protocol name."""

    return dict(outputs)
