# Outputs

This directory is reserved for generated benchmark artifacts and analysis outputs.

Expected subdirectories:

- `manifests/`
- `reports/`
- `figures/`
- `tables/`

These paths are scaffolded now so future scripts can write to stable locations.
